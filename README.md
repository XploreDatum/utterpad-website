# utterpad.com

Static landing page for [UtterPad](https://github.com/XploreDatum/UtterPad), deployed to Cloudflare Pages.

Pure HTML/CSS/JS — no build step. `wrangler pages deploy .` from this directory.
